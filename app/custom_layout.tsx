"use client";

import React from "react";
import { ToastContainer } from "react-toastify";

const CustomLayout = () => {
    React.useEffect(() => {
        ToastContainer;
    }, []);

    return (
        <React.Fragment>
            <ToastContainer />
        </React.Fragment>
    );
};

export default CustomLayout;
